package com.example.algostudy.repository;

import com.example.algostudy.domain.entity.MissionCalander;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MissionCalanderRepository extends JpaRepository<MissionCalander, Long> {
}
